<form method="post" action="{{ path('app_task_blueprint_delete', {'id': task_blueprint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task_blueprint.id) }}">
    <button class="btn">Delete</button>
</form>
