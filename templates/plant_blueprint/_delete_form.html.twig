<form method="post" action="{{ path('app_plant_blueprint_delete', {'id': plant_blueprint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plant_blueprint.id) }}">
    <button class="btn">Delete</button>
</form>
