<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>
			{% block title %}Welcome!
			{% endblock %}
			● Symfony Garden
		</title>
		<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
		<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
		<link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
		<link rel="manifest" href="/site.webmanifest">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous"> {% block stylesheets %}{% endblock %}
    </head>
    <body>

        <!-- NAVBAR -->
        <nav class="navbar navbar-expand-lg bg-primary mb-4">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">Symfony Garden</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_dashboard') }}">Tableau de bord</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_plant_index') }}">Mes plantes</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_task_index') }}">Mes tâches</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Base de plantes
                            </a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_plant_blueprint_index') }}">Tout voir</a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_plant_blueprint_new') }}">Ajouter une espèce de plante</a>
                                </li>
                            </ul>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Base de tâches
                            </a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_task_blueprint_index') }}">Tout voir</a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_task_blueprint_new') }}">Ajouter un modèle de tâche</a>
                                </li>
                            </ul>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Annexes
                            </a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_category_index') }}">Catégories</a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_family_index') }}">Familles</a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_tag_index') }}">Étiquettes</a>
                                </li>
                            </ul>
                        </li>
                    </ul>

                    <span class="navbar-text me-3">
                        Bonjour,
                        {{ app.user.firstName }}
                    </span>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Déconnexion</a>
                </div>
            </div>
        </nav>

        <div class="container mt-5">
            {% block body %}
            {% endblock %}
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    </body>
</html>
